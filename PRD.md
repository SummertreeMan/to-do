# PRD: Personal To-do List Web App

## 1) 제품 개요

### 1.1 목적
- 개인 사용자가 할 일을 빠르게 기록하고 관리할 수 있는 경량 웹 앱을 제공한다.
- 초기 버전은 브라우저 `localStorage`를 사용해 즉시 동작하도록 구현한다.
- 이후 Firebase Firestore로 확장 가능한 구조를 유지한다.
- Vercel 배포를 기준으로 개발/운영 환경을 단순화한다.

### 1.2 문제 정의
- 메모 앱이나 복잡한 생산성 도구는 개인의 빠른 할 일 관리에 과도한 기능을 요구한다.
- 사용자는 설치/회원가입 없이 바로 사용할 수 있는 단순한 To-do 도구를 필요로 한다.

## 2) 대상 사용자
- 개인 일정/할 일을 스스로 관리하려는 단일 사용자
- 로그인 없이 빠르게 사용하고 싶은 사용자
- 추후 여러 기기 동기화(Firestore 확장)를 고려하는 사용자

## 3) 범위

### 3.1 포함 범위 (MVP)
- 할 일 생성
- 할 일 목록 조회
- 할 일 완료/미완료 토글
- 할 일 삭제
- 기본 필터(전체/완료/미완료)
- `localStorage` 기반 데이터 영속성
- Firestore 확장을 고려한 저장소 추상화(인터페이스 분리)
- Vercel 배포 가능 상태 확보

### 3.2 제외 범위
- 로그인/회원가입
- 권한 관리(역할, 접근 제어)
- 복잡한 CRUD(서브태스크, 대량 편집, 이력 관리, 복구, 버전 관리)
- 협업 기능(공유, 초대, 코멘트)
- 알림, 캘린더 연동, 파일 첨부

## 4) 핵심 사용자 시나리오
- 사용자는 앱에 접속해 할 일 제목을 입력하고 추가한다.
- 사용자는 목록에서 항목을 보고 완료 여부를 토글한다.
- 사용자는 불필요한 항목을 삭제한다.
- 사용자는 새로고침 후에도 저장된 목록을 다시 확인한다.
- 사용자는 완료 상태에 따라 목록을 필터링해 확인한다.

## 5) 기능 요구사항

### 5.1 To-do 관리
- 사용자는 텍스트 입력 후 할 일을 추가할 수 있어야 한다.
- 빈 문자열 또는 공백-only 입력은 저장하지 않아야 한다.
- 각 할 일은 최소 다음 필드를 가져야 한다.
  - `id: string`
  - `title: string`
  - `completed: boolean`
  - `createdAt: number` (epoch ms)
- 사용자는 할 일의 완료 상태를 토글할 수 있어야 한다.
- 사용자는 할 일을 삭제할 수 있어야 한다.

### 5.2 필터
- 필터는 `all | active | completed` 3가지를 제공해야 한다.
- 필터 전환 시 조건에 맞는 목록만 표시해야 한다.

### 5.3 저장소
- 앱 시작 시 `localStorage`에서 목록을 로드해야 한다.
- 목록 변경(추가/토글/삭제) 시 즉시 `localStorage`에 반영해야 한다.
- 저장소 계층은 추후 Firestore로 교체 가능한 형태여야 한다.
  - 예: `TodoRepository` 인터페이스 + `LocalStorageTodoRepository` 구현체

### 5.4 배포
- Next.js 빌드가 통과해야 한다.
- Vercel 환경에서 별도 서버 설정 없이 동작해야 한다.
- Firestore 확장 시 필요한 환경 변수 키를 문서화해야 한다(초기에는 선택사항).

## 6) 비기능 요구사항
- 초기 로딩 후 첫 할 일 입력까지의 UX가 직관적이어야 한다.
- 주요 사용자 액션(추가/토글/삭제)은 즉시 화면에 반영되어야 한다.
- TypeScript strict mode에 맞는 타입 안정성을 유지해야 한다.
- 코드 구조는 함수형 패턴 중심(순수 함수/불변 업데이트 우선)으로 작성한다.

## 7) 데이터 모델 (초안)

```ts
export type Todo = {
  id: string;
  title: string;
  completed: boolean;
  createdAt: number;
};

export type TodoFilter = "all" | "active" | "completed";
```

## 8) 확장 전략 (Firestore)

### 8.1 단계적 전환
- 1단계: `localStorage` 단독 사용
- 2단계: Firestore 저장소 구현체 추가
- 3단계: 환경 설정에 따라 저장소 선택(또는 마이그레이션 전략 도입)

### 8.2 고려사항
- 컬렉션 예시: `todos/{todoId}`
- 향후 사용자 식별 도입 전까지는 단일 사용자 문맥을 가정
- 로그인 기능은 본 PRD 범위에서 제외

## 9) 성공 지표 및 완료 기준

### 9.1 성공 지표
- 사용자가 페이지 진입 후 5초 이내 첫 할 일을 추가 가능
- 새로고침 이후 데이터 유지 성공률 100%
- 핵심 시나리오(추가/토글/삭제/필터) 수동 테스트 통과

### 9.2 완료 기준 (Definition of Done)
- [ ] 할 일 추가/조회/토글/삭제 기능 구현 완료
- [ ] 필터(`all/active/completed`) 구현 완료
- [ ] `localStorage` 영속성 구현 완료
- [ ] 저장소 추상화 구조 반영(향후 Firestore 확장 가능)
- [ ] Vercel 배포 후 동작 검증 완료
- [ ] README에 실행 방법 및 배포/확장 메모 반영

## 10) 리스크 및 대응
- 브라우저 저장소 의존으로 기기 간 동기화 불가
  - 대응: Firestore 저장소 확장 로드맵 유지
- 저장 데이터 스키마 변경 시 호환성 이슈 가능
  - 대응: 버전 키 또는 안전한 파싱/기본값 처리
